<div class="container">
    <div class="jumbotron">

        <div class='py-9'>
            <h1 class="text-center my-5">Scraper for Medium Addicts!!</h1>
        </div>

        {{#each user.article}} 
        {{#unless saved}} 
        {{>articles/cardArticles this _id=_id title=title summary=summary link=link saved=true}}
        {{/unless}} 
        {{/each}}

    </div>
</div>